// Placeholder root page — a later task replaces this with the real
// TimesheetsPage. This file only exists so `next build` has something to render.
export default function Page() {
  return null;
}
